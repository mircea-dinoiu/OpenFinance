export enum QueryParam {
    endDate = 'endDate',
    include = 'include',
    includePending = 'includePending',

    sorters = 'sorters',
    filters = 'filters',
    pageSize = 'pageSize',
    page = 'page',
}

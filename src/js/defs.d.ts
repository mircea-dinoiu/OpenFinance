export enum RepeatOption {
    DAY = 'd',
    WEEK = 'w',
    MONTH = 'm',
    YEAR = 'y',
}

export const paths: {
    home: string;
    dashboard: string;
    transactions: string;
    login: string;
};

export const PERC_MAX: number;
export const PERC_STEP: number;
